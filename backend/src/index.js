#!/usr/bin/env node

/**
 * Placeholder bootstrap so the backend folder can run independently during early planning.
 * Replace this with the real HTTP server once the architecture is settled.
 */
console.log("FocusBadge backend placeholder server — implement API here.");
